appId: com.example.cliniko-capacity
productName: Cliniko Capacity
directories:
  output: release
# Bundled main/preload live in build/; the renderer is copied in as a resource.
files:
  - build/**
extraResources:
  - from: renderer
    to: renderer
# Auto-update source. Set these to your GitHub repo; a tagged release published
# here is what installed apps check against.
publish:
  provider: github
  owner: YOUR_GITHUB_USERNAME
  repo: YOUR_REPO_NAME
mac:
  target: dmg
  category: public.app-category.healthcare-fitness
  # Unsigned for now: `identity: null` tells electron-builder to skip signing
  # (so CI doesn't fail looking for a certificate). macOS auto-update stays off
  # until you sign + notarize. To sign later, remove this line and set:
  #   identity: "Developer ID Application: Your Name (TEAMID)"
  # plus the CSC_*/APPLE_* secrets in .github/workflows/release.yml.
  identity: null
win:
  target: nsis
linux:
  target: AppImage
